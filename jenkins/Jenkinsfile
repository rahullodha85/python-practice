pipeline {
    stages {
        stage('Build Docker Image') {
            steps {
                echo 'Building Docker Image'
                script {
                    app = docker.build('rahullodha85/python-practice')
                }
            }
        }
    }
}